id: list
label: List
name: list
description: Inserts a list.
sample: |
  [{{ name }}=lower-roman]
    [*] One fish
    [*] Two fish
  [/{{ name }}]
template_file: '@xbbcode_standard/list.html.twig'
dependencies:
  enforced:
    module:
      - xbbcode_standard
